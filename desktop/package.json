{
    "scripts": {
        "start": "rm -rf ./resources/assets/* && cp -r ../dist/* ./resources && node ./scripts/AddScriptTagToIndexHTML.js && neu run"
    },
    "devDependencies": {
        "shx": "^0.3.4"
    }
}
